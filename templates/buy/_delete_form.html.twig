<form method="post" action="{{ path('app_buy_delete', {'id': buy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ buy.id) }}">
    <button class="btn">Delete</button>
</form>
